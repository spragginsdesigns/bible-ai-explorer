# Expo / React Native
node_modules/
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `npx expo prebuild`
/android/
/ios/

# Local env
.env*.local

# Misc
*.log
.DS_Store
*.apk
.phone-addr

# Firebase config for FCM push (contains project API keys, not user secrets, but keep out of VCS per convention)
google-services.json
